import { apolloProvider } from './apollo'
import { fb, db, auth, messaging, currentUser } from './adminFirebase'

export {
    fb,
    db,
    auth,
    messaging,
    currentUser,
    apolloProvider,
}